{
    "version": "0.1.0",
    "configurations": [
        {
            "name": "Run mocha Mock.test",
            "type": "node",
            "program": "${workspaceRoot}/node_modules/mocha/bin/_mocha",
            "stopOnEntry": false,
            "args": ["${workspaceRoot}/.tmp/test/Mock.test.js"],
            "cwd": "${workspaceRoot}/",
            "runtimeExecutable": null,
            "env": { "NODE_ENV": "production"}
        }
    ]
}